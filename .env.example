# Package Configuration
PACKAGE_NAME=graphql-mcp-tools
PACKAGE_VERSION=1.0.1
PACKAGE_DESCRIPTION=GraphQL API integration tools for MCP

# GraphQL API Configuration
ENDPOINT=http://localhost:3000/graphql

# Optional: Custom headers for GraphQL requests (JSON format)
# Example: {"Authorization": "Bearer your-token"}
# HEADERS=

# Optional: Maximum response depth for GraphQL queries
# MAX_RESPONSE_DEPTH=2
